databases:
  - name: tripmate
    databaseName: tripmate_prod
    user: tripmateuserprod

services:
  - type: web
    name: tripmate
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tripmate_prod
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false