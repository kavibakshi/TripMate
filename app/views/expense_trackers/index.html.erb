<div class="d-flex align-items-center justify-content-center">
    <h1 class="title ">My Expense Tracker</h1>     
</div>
<div><%= link_to "+ Add New Expense Details", new_expense_tracker_path, class: "btn btn-warning" %></div></br>
<div class="container">
  <div class="row">
    <div class="col-12" style="">
      <table class="table caption-top table-hover">
        <thead>
          <tr>
            <th>Itinerary ID</th>
            <th>Itinerary Name</th>
            <th>Expense ID</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
        <% if @expense_lists.exists? %>
          <%= render "expense_list" %>
        <% else %>
          <tr><td colspan="">Sorry, there's no itinerary found...</td></tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
